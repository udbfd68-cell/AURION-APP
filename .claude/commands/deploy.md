Déploie le projet sur Vercel.

1. `npx tsc --noEmit` → vérifier 0 erreurs TypeScript
2. `npm run build` → vérifier compilation réussie
3. `npx vercel --prod` → déployer
4. Afficher l'URL finale
5. Rappeler de vérifier manuellement : console Chrome = 0 erreurs
